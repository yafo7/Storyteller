{
  "fixtureVersion": "1.0.0",
  "fixtureId": "fixture.maturity.closed-archive-route",
  "patternId": "pattern.capability-recontextualizes-old-space",
  "testId": "test.capability-revisit.route",
  "kind": "route",
  "status": "authored-contract-fixture",
  "originalityStatement": "This synthetic contract fixture was authored for Storyteller validation and contains no source map, character, dialogue, audiovisual signature, or encounter sequence.",
  "purpose": "route contract fixture for pattern.capability-recontextualizes-old-space; it defines an original scenario boundary and expected observable evidence, not a claim that a game runtime has already executed it.",
  "sourceContract": "packs/zelda-mainline/patterns/maturity-contracts.json",
  "scenarioBoundary": {
    "subtype": "capability-gated-return",
    "selectionRole": "core",
    "requiredRuntimeCapabilities": [
      "capability.world-facts",
      "capability.route-gates",
      "capability.map-layers",
      "capability.route-simulation"
    ],
    "recoveryRequirements": [
      "Keep a bidirectional escape edge",
      "Preserve capability and learned affordance facts after failure",
      "Disable the new edge if its destination cannot load"
    ]
  },
  "expectedAssertions": [
    {
      "evidencePath": "$.routes.firstVisit.requiredGoal",
      "operator": "reachable",
      "expected": true
    },
    {
      "evidencePath": "$.routes.revisit.newEdge",
      "operator": "reachable",
      "expected": true
    },
    {
      "evidencePath": "$.routes.revisit.returnEdge",
      "operator": "reachable",
      "expected": true
    }
  ],
  "executionPolicy": {
    "executableInP4": false,
    "firstRuntimeExecutionPhase": "P7",
    "failClosedUntilRuntimeEvidence": true
  }
}
