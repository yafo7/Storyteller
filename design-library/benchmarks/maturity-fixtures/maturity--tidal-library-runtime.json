{
  "fixtureVersion": "1.0.0",
  "fixtureId": "fixture.maturity.tidal-library-runtime",
  "patternId": "pattern.state-axis-rewrites-routes",
  "testId": "test.state-axis.runtime",
  "kind": "runtime",
  "status": "authored-contract-fixture",
  "originalityStatement": "This synthetic contract fixture was authored for Storyteller validation and contains no source map, character, dialogue, audiovisual signature, or encounter sequence.",
  "purpose": "runtime contract fixture for pattern.state-axis-rewrites-routes; it defines an original scenario boundary and expected observable evidence, not a claim that a game runtime has already executed it.",
  "sourceContract": "packs/zelda-mainline/patterns/maturity-contracts.json",
  "scenarioBoundary": {
    "subtype": "discrete-route-axis",
    "selectionRole": "core",
    "requiredRuntimeCapabilities": [
      "capability.world-facts",
      "capability.map-layers",
      "capability.route-gates",
      "capability.checkpoints",
      "capability.route-simulation"
    ],
    "recoveryRequirements": [
      "Atomic rollback of layer and route table",
      "Emergency return to last transition checkpoint",
      "Preserve learned state labels"
    ]
  },
  "expectedAssertions": [
    {
      "evidencePath": "$.layers.visualCollisionInteractionAgree",
      "operator": "equals",
      "expected": true
    },
    {
      "evidencePath": "$.effects.rollbackRestoresPriorValue",
      "operator": "equals",
      "expected": true
    }
  ],
  "executionPolicy": {
    "executableInP4": false,
    "firstRuntimeExecutionPhase": "P7",
    "failClosedUntilRuntimeEvidence": true
  }
}
